# Ecom

This is a fullstack e-commerce project with separate `frontend` and `backend` folders.

## Features
- Frontend: React
- Backend: Node.js, Express
- Database: MongoDB

## Instructions
1. Navigate to the `frontend` folder and run `npm start`.
2. Navigate to the `backend` folder and run `npm start`.
